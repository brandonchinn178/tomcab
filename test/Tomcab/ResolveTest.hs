{- AUTOCOLLECT.TEST -}

module Tomcab.ResolveTest (
  -- $AUTOCOLLECT.TEST.export$
) where

{----- resolveImports -----}

test_todo = "resolve imports: non-existent common stanza"
test_todo = "resolve imports: merge lists"
test_todo = "resolve imports: common stanza fields overridden by latter common stanzas"
test_todo = "resolve imports: common stanza fields overridden by set field"

{----- resolveModules -----}

test_todo = "resolve modules: no wildcards"
test_todo = "resolve modules: plain wildcards"
test_todo = "resolve modules: wildcard overridden with explicit"
test_todo = "resolve modules: wildcard overridden with other-modules"
